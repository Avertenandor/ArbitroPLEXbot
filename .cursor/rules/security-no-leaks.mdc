# 🔒 КРИТИЧНОЕ ПРАВИЛО: ЗАЩИТА СЕКРЕТОВ И ДОКУМЕНТАЦИИ

## АБСОЛЮТНЫЙ ЗАПРЕТ НА КОММИТЫ

### ❌ НИКОГДА не добавлять в Git:

#### 1. Секреты и ключи
- `.env`, `.env.*`, `*.env` - любые файлы переменных окружения
- `secrets.json`, `secrets*.json`, `*_secrets.json`
- `credentials.json`, `credentials*.json`
- `*.pem`, `*.key`, `*.p12`, `*.pfx` - криптографические ключи
- `private_key*`, `*_private*` - приватные ключи
- `*secret*`, `*token*`, `api_keys*` - любые файлы с секретами/токенами
- `keys.json` - ключи доступа

#### 2. Документация сервера (СТРОГО ЛОКАЛЬНО!)
- `SERVER_INFO.md` - данные сервера (IP, токены, доступы)
- `SERVER_ACCESS.md` - инструкции по подключению
- `DEPLOY_INFO.md` - данные деплоя
- `CREDENTIALS.md`, `KEYS.md` - любые креды
- `*_ACCESS.md`, `*_INFO.md` - подобные файлы
- `docs/private/`, `docs/secrets/`, `docs/deploy/` - приватные папки

#### 3. Локальные конфиги
- `config.local.*`, `*.local.json`, `*.local.yml`, `*.local.yaml`
- `settings.local.*`
- `production.env`, `staging.env` - окружения с реальными данными

#### 4. Базы данных и логи
- `*.db`, `*.sqlite`, `*.sqlite3` - файлы БД
- `data/`, `database/` - папки с данными
- `*.log`, `logs/`, `*.jsonl` - логи

#### 5. SSH ключи
- `*.ppk`, `id_rsa`, `id_rsa.pub`, `id_ed25519`, `id_ed25519.pub`
- `google_compute_engine*` - ключи GCP

## ✅ ОБЯЗАТЕЛЬНЫЕ ПРОВЕРКИ ПЕРЕД КОММИТОМ

### 1. Перед `git add`:
```bash
# Проверь что в индексе нет запрещённых файлов
git status -sb
git ls-files | findstr /i "server_info .env secret credential"
```

**Если команда что-то нашла → СТОП! Не коммить!**

### 2. Проверь содержимое файлов:
- ❌ Нет хардкода токенов Telegram, API ключей
- ❌ Нет адресов RPC/WSS с ключами в URL
- ❌ Нет приватных ключей кошельков
- ❌ Нет паролей, хешей, солей
- ❌ Нет IP адресов продакшен серверов
- ✅ Все секреты читаются из `os.getenv()` или `settings`

### 3. Убедись что .gitignore актуален:
```bash
# Проверь что файл есть и содержит все запреты
cat .gitignore | findstr /i "SERVER_INFO .env secret"
```

## 🚨 ЧТО ДЕЛАТЬ ПРИ ОБНАРУЖЕНИИ УТЕЧКИ

### Если секрет уже в Git:

1. **НЕ делай force push!** (это оставит след в reflog)
2. **Используй `git filter-repo` или `BFG Repo-Cleaner`:**
   ```bash
   # Удалить файл из всей истории
   git filter-repo --invert-paths --path SERVER_INFO.md
   ```
3. **Смени скомпрометированные секреты:**
   - Telegram Bot Token → создай нового бота или обнови токен через BotFather
   - API ключи NodeReal/Etherscan → выпусти новые
   - Приватные ключи кошельков → СРОЧНО выведи активы, сгенерируй новые

4. **Уведоми команду** о необходимости обновить `.env` локально

## 📋 ШАБЛОН ПРОВЕРКИ ДЛЯ AI АГЕНТА

Перед каждым `git add` / `git commit`:

```markdown
✅ Проверил `git status` - нет файлов из запретного списка
✅ Проверил содержимое изменённых файлов - нет хардкодов секретов
✅ Убедился что SERVER_INFO.md в .gitignore и НЕ в индексе
✅ Убедился что все `.env*` файлы в .gitignore
✅ Проверил что новые файлы не содержат токенов/ключей/IP
```

**Если хотя бы один пункт НЕ выполнен → НЕ коммитить!**

## 🔐 ПРАВИЛА ДЛЯ КОДА

### В коде ВСЕГДА:
```python
# ✅ ПРАВИЛЬНО - через переменные окружения
BOT_TOKEN = os.getenv("BOT_TOKEN")
RPC_URL = settings.BSC_RPC_URL
PRIVATE_KEY = settings.WALLET_PRIVATE_KEY

# ❌ НЕПРАВИЛЬНО - хардкод
BOT_TOKEN = "1234567890:ABCdefGHIjklMNOpqrsTUVwxyz"
RPC_URL = "https://bsc-mainnet.nodereal.io/v1/SECRET_KEY"
PRIVATE_KEY = "0xabcdef1234567890..."
```

### Для констант (контракты, адреса):
```python
# ✅ МОЖНО - публичные адреса контрактов
PLEX_CONTRACT = "0xdf179b6cadbc61ffd86a3d2e55f6d6e083ade6c1"
USDT_CONTRACT = "0x55d398326f99059ff775485246999027b3197955"

# ❌ НЕЛЬЗЯ - приватные ключи, admin IDs
ADMIN_WALLET_KEY = "0x..."  # НЕТ!
```

## 📚 ДОПОЛНИТЕЛЬНО

### Структура секретов:
```
project/
├── .env                    # локально, в .gitignore
├── .env.example            # шаблон (БЕЗ реальных значений) - можно в Git
├── SERVER_INFO.md          # локально, в .gitignore
└── README.md               # инструкция как создать .env - можно в Git
```

### Пример .env.example (для Git):
```env
# Telegram Bot
BOT_TOKEN=your_bot_token_here

# Blockchain
BSC_RPC_URL=https://bsc-dataseed.binance.org/
BSC_WSS_URL=wss://bsc-ws-node.nariox.org:443

# Wallet
MAIN_WALLET_ADDRESS=0x...
MAIN_WALLET_PRIVATE_KEY=your_private_key_here
```

## ⚡ АВТОМАТИЗАЦИЯ

### Pre-commit hook (опционально):
```bash
#!/bin/bash
# .git/hooks/pre-commit

# Проверка на секреты
if git diff --cached --name-only | grep -E "(SERVER_INFO|\.env|secret|credential)"; then
    echo "❌ ОШИБКА: Попытка закоммитить секреты!"
    exit 1
fi

# Проверка на хардкод токенов (простой regex)
if git diff --cached | grep -E "[0-9]{10}:[A-Za-z0-9_-]{35}"; then
    echo "❌ ОШИБКА: Обнаружен Telegram Bot Token в коде!"
    exit 1
fi

exit 0
```

---

## 🎯 РЕЗЮМЕ ДЛЯ AI АГЕНТА

**Золотое правило:** Если не уверен можно ли коммитить файл → **НЕ коммитить!**

**Перед каждым `git push`:**
1. Проверь `git log --name-only -1` - только код, нет секретов
2. Проверь `git diff HEAD^ HEAD` - нет токенов/ключей в diff
3. Проверь `.gitignore` - актуален

**При малейшем сомнении → спроси пользователя!**
