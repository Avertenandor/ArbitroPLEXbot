---
description: Правила безопасности для ArbitroPLEXbot - запрет на отправку секретов и документации в репозиторий
globs: ["**/*"]
alwaysApply: true
---

# 🔒 ПРАВИЛА БЕЗОПАСНОСТИ ArbitroPLEXbot

## КРИТИЧЕСКИЕ ЗАПРЕТЫ

### 1. НИКОГДА не добавлять в Git:
- `.env` файлы и любые файлы с переменными окружения
- `SERVER_INFO.md`, `SERVER_ACCESS.md` и любую документацию с данными сервера
- SSH ключи, API токены, пароли
- Любые файлы содержащие `token`, `secret`, `password`, `key`, `credential`

### 2. Перед любым git add/commit/push:
- Проверить что файл НЕ содержит секретов
- Убедиться что файл НЕ в списке запрещённых
- Проверить `.gitignore` включает файл

### 3. Репозиторий проекта:
- URL: https://github.com/Avertenandor/ArbitroPLEXbot
- Ветка: main
- ТОЛЬКО код, без конфигов и документации сервера

## СПИСОК ЗАПРЕЩЁННЫХ ФАЙЛОВ

```
.env, .env.*, *.env
SERVER_INFO.md, SERVER_ACCESS.md, DEPLOY_INFO.md
secrets.json, credentials.json, keys.json
*.pem, *.key, *.ppk
config.local.*, *.local.json
```

## ДАННЫЕ СЕРВЕРА (НЕ КОММИТИТЬ!)

- Instance: `instance-20251107-043512`
- IP: `35.228.48.9`
- Token бота хранится ТОЛЬКО в `.env` локально

## ПРИ РАБОТЕ С КОДОМ

1. Все секреты читать из переменных окружения: `os.getenv("VAR_NAME")`
2. Никогда не хардкодить токены/пароли в код
3. Использовать `.env.example` для документации переменных (без реальных значений)
