# Deposit Settings Module

## Описание

Модуль управления настройками депозитов для администраторов.
Позволяет настраивать максимальный уровень депозита и управлять
доступностью уровней.

**Особенности:**
- R17-2: Временная деактивация уровней через флаг is_active
- Управление коридорами депозитов
- Управление ставками PLEX

## Структура модуля

Модуль разбит на несколько подмодулей для лучшей организации кода:

### 📄 `__init__.py` (28 строк)
Главный модуль, объединяющий все роутеры. Экспортирует единый
`router` для обратной совместимости.

### 📄 `constants.py` (11 строк)
Содержит константы, используемые в модуле:
- `LEVEL_EMOJI`: Mapping эмодзи для уровней депозитов

### 📄 `display.py` (204 строки)
Обработчики отображения информации:
- `show_deposit_settings`: Показ настроек уровней депозитов
- `show_deposit_statistics`: Статистика депозитов по уровням
- `show_level_status`: Детальный статус всех уровней

### 📄 `management.py` (183 строки)
Обработчики управления уровнями:
- `set_max_deposit_level`: Установка максимального уровня депозита
- `toggle_level_availability`: Включение/отключение уровней

### 📄 `parameters.py` (222 строки)
Обработчики управления параметрами:
- `update_corridor`: Обновление коридора депозита для уровня
- `update_plex_rate`: Обновление ставки PLEX для всех уровней

### 📄 `navigation.py` (22 строки)
Обработчики навигации:
- `handle_back_to_admin_panel`: Возврат в админ-панель

## Использование

### Импорт модуля

```python
# Импорт всего модуля (для регистрации роутера)
from bot.handlers.admin import deposit_settings
dp.include_router(deposit_settings.router)

# Импорт конкретных функций
from bot.handlers.admin.deposit_settings.display import (
    show_deposit_settings
)
```

### Команды для пользователей

**Кнопки:**
- "⚙️ Настроить уровни депозитов" - главное меню настроек
- "📊 Статистика депозитов" - статистика по уровням
- "📊 Статус уровней" - детальный статус
- "👑 Админ-панель" - возврат в админ-панель

**Текстовые команды:**
- `коридор <уровень> <мин> <макс>` - изменить коридор
- `включить <уровень>` - включить уровень
- `отключить <уровень>` - отключить уровень
- `plex <значение>` - изменить PLEX за $1
- `уровень <номер>` - установить максимальный уровень (1-5)

**Примеры:**
```
коридор test 30 100
коридор level_1 100 500
включить level_2
отключить test
plex 15
уровень 3
```

## Требования

- ✅ Все файлы < 500 строк
- ✅ Все строки < 100 символов
- ✅ Функционал полностью сохранен
- ✅ Обратная совместимость обеспечена

## История изменений

### 2025-12-13: Рефакторинг
- Разбит исходный файл (547 строк) на 6 модулей
- Улучшена читаемость и maintainability кода
- Сохранена обратная совместимость через `__init__.py`
- Все строки приведены к лимиту 100 символов
