# ТЗ: Анализ и исправление раздела "Управление кошельком"

## Контекст проблемы
При переходе в админ-панели в раздел "🔐 Управление кошельком" возникает ошибка парсинга Markdown на byte offset 46. Раздел не работает корректно.

## Цель
Провести полный анализ всех файлов, связанных с функциональностью "Управление кошельком", выявить все ошибки и несоответствия, предложить исправления.

---

## ЗАДАЧИ ДЛЯ АГЕНТОВ (20 параллельных)

### Группа 1: Анализ хендлеров (5 агентов)

#### Агент 1: Основной хендлер wallet_management.py
**Файл:** `bot/handlers/admin/wallet_management.py`
**Задачи:**
1. Проверить все роуты на корректность фильтров (F.text ==)
2. Найти все места с форматированием Markdown - проверить на корректность escape-символов
3. Проверить все вызовы `message.answer()` с `parse_mode="Markdown"` на правильность форматирования
4. Найти проблемы с вложенными спецсимволами: `_`, `*`, `` ` ``, `(`, `)` внутри Markdown
5. Проверить работу с `state.set_state()` и `state.get_data()`

#### Агент 2: Навигация в панели
**Файл:** `bot/handlers/admin/panel/navigation.py`
**Задачи:**
1. Проверить роут `@router.message(F.text == "🔐 Управление кошельком")`
2. Проанализировать конфликты с другими хендлерами на тот же текст
3. Проверить порядок регистрации роутеров
4. Найти возможные дублирующиеся обработчики

#### Агент 3: Авторизация админа
**Файл:** `bot/handlers/admin/panel/auth.py`
**Задачи:**
1. Проверить логику редиректа `redirect_message_text == "🔐 Управление кошельком"`
2. Проанализировать flow авторизации master key
3. Проверить сохранение/восстановление state после авторизации
4. Найти edge cases при авторизации

#### Агент 4: Главная панель админа
**Файл:** `bot/handlers/admin/panel/main.py`
**Задачи:**
1. Проверить инициализацию админ-панели
2. Найти места, где может теряться state
3. Проверить взаимодействие с WalletManagementStates

#### Агент 5: Смежные хендлеры кошельков
**Файлы:** 
- `bot/handlers/admin/wallets.py`
- `bot/handlers/admin/wallet_key_setup/` (все файлы в директории)
**Задачи:**
1. Проверить на конфликты с wallet_management.py
2. Найти пересекающиеся роуты
3. Проверить использование общих состояний

---

### Группа 2: Анализ состояний и клавиатур (4 агента)

#### Агент 6: States для wallet management
**Файл:** `bot/states/wallet_management.py`
**Задачи:**
1. Проверить определение всех состояний
2. Найти неиспользуемые состояния
3. Проверить naming convention
4. Сравнить с фактическим использованием в хендлерах

#### Агент 7: Клавиатуры админа
**Файлы:**
- `bot/keyboards/admin/main_keyboards.py`
- `bot/keyboards/buttons/admin.py`
**Задачи:**
1. Проверить определение кнопки "🔐 Управление кошельком"
2. Проверить клавиатуру wallet_dashboard_keyboard()
3. Найти несоответствия между текстом кнопок и фильтрами в хендлерах

#### Агент 8: Утилиты меню
**Файл:** `bot/utils/menu_buttons.py`
**Задачи:**
1. Проверить список ADMIN_MENU_BUTTONS
2. Найти дублирования кнопок
3. Проверить использование в коде

#### Агент 9: Все состояния админа
**Файлы:** `bot/states/` (все файлы)
**Задачи:**
1. Найти все StatesGroup связанные с админкой
2. Проверить возможные конфликты состояний
3. Проверить импорты состояний

---

### Группа 3: Анализ сервисов и моделей (4 агента)

#### Агент 10: Blockchain сервис
**Файлы:**
- `app/services/blockchain/service_facade.py`
- `app/services/blockchain/token_service.py`
- `app/services/blockchain/native_service.py`
**Задачи:**
1. Проверить методы get_balance для BNB, USDT, PLEX
2. Найти места где может быть None вместо баланса
3. Проверить обработку исключений
4. Проверить форматирование возвращаемых значений

#### Агент 11: Конфигурация и настройки
**Файлы:**
- `app/config/settings.py`
- `app/config/blockchain.py`
**Задачи:**
1. Проверить переменные TREASURY_ADDRESS, COLD_WALLET_ADDRESS
2. Найти места где hot_address может быть None
3. Проверить валидацию адресов кошельков

#### Агент 12: Репозитории депозитов
**Файлы:**
- `app/repositories/deposit_level_config_repository.py`
- `app/repositories/base.py`
**Задачи:**
1. Проверить метод find_all() (ранее была ошибка get_all)
2. Проверить наследование от BaseRepository
3. Найти отсутствующие методы

#### Агент 13: AI Wallet Service
**Файл:** `app/services/ai_wallet_service.py`
**Задачи:**
1. Проверить метод get_plex_rate() - была ошибка get_all
2. Проверить check_user_wallet()
3. Найти все места вызова методов репозиториев

---

### Группа 4: Анализ middlewares и инициализации (4 агента)

#### Агент 14: Middleware авторизации
**Файлы:**
- `bot/middlewares/admin_auth_middleware.py`
- `bot/middlewares/auth.py`
**Задачи:**
1. Проверить логику определения is_admin
2. Найти места где может теряться admin_id
3. Проверить bypass для WalletManagementStates

#### Агент 15: Database и Session middleware
**Файлы:**
- `bot/middlewares/database.py`
- `bot/middlewares/session_middleware.py`
**Задачи:**
1. Проверить обработку ошибок (Telegram parse errors)
2. Найти места логирования ошибок
3. Проверить передачу session в хендлеры

#### Агент 16: Error handler middleware
**Файл:** `bot/middlewares/error_handler.py`
**Задачи:**
1. Проверить обработку Markdown parse errors
2. Найти fallback логику при ошибках
3. Проверить recovery после ошибок

#### Агент 17: Регистрация хендлеров
**Файл:** `bot/initialization/handlers.py`
**Задачи:**
1. Проверить порядок регистрации admin handlers
2. Найти порядок router.include_router()
3. Проверить что wallet_management регистрируется корректно

---

### Группа 5: Тестирование и документация (3 агента)

#### Агент 18: Тесты wallet
**Файлы:** `tests/` (все файлы связанные с wallet)
**Задачи:**
1. Найти существующие тесты для wallet_management
2. Проверить покрытие тестами
3. Предложить недостающие тест-кейсы

#### Агент 19: Поиск похожих ошибок
**Файлы:** Все `bot/handlers/admin/*.py`
**Задачи:**
1. Найти все места с parse_mode="Markdown"
2. Проверить каждое на потенциальные ошибки форматирования
3. Найти паттерны `_(...текст...)_` или `*(...текст...)*`
4. Составить список всех рискованных мест

#### Агент 20: Интеграционный анализ
**Задачи:**
1. Построить flow диаграмму: нажатие кнопки → хендлер → ответ
2. Найти все точки где может прерваться flow
3. Проверить consistency между разными файлами
4. Составить итоговый отчёт по всем найденным проблемам

---

## Файлы для анализа (полный список)

### Хендлеры (высокий приоритет)
```
bot/handlers/admin/wallet_management.py
bot/handlers/admin/panel/navigation.py
bot/handlers/admin/panel/auth.py
bot/handlers/admin/panel/main.py
bot/handlers/admin/wallets.py
bot/handlers/admin/wallet_key_setup/__init__.py
bot/handlers/admin/wallet_key_setup/*.py
```

### States и Keyboards
```
bot/states/wallet_management.py
bot/states/admin.py
bot/keyboards/admin/main_keyboards.py
bot/keyboards/buttons/admin.py
bot/utils/menu_buttons.py
```

### Сервисы
```
app/services/blockchain/service_facade.py
app/services/blockchain/token_service.py
app/services/blockchain/native_service.py
app/services/ai_wallet_service.py
```

### Конфигурация
```
app/config/settings.py
app/config/blockchain.py
```

### Репозитории
```
app/repositories/base.py
app/repositories/deposit_level_config_repository.py
```

### Middlewares
```
bot/middlewares/admin_auth_middleware.py
bot/middlewares/auth.py
bot/middlewares/database.py
bot/middlewares/session_middleware.py
bot/middlewares/error_handler.py
```

### Инициализация
```
bot/initialization/handlers.py
bot/initialization/middlewares.py
```

---

## Известные проблемы (уже исправлены, но проверить)

1. **Markdown parse error offset 46** - `_(текст)_` вызывает ошибку парсинга
2. **`get_all` не существует** - должен быть `find_all()` в BaseRepository
3. **`count_all` не существует** - должен быть `count()` 
4. **`count_active` не существует** - должен быть `count_verified_users()`
5. **SQL GroupingError** - в hourly_activity нужно использовать одну переменную для group_by

---

## Формат отчёта от каждого агента

```markdown
## Агент N: [Название задачи]

### Проанализированные файлы
- файл1.py
- файл2.py

### Найденные проблемы
1. **[CRITICAL]** Описание проблемы
   - Файл: path/to/file.py
   - Строка: XX
   - Код: `проблемный код`
   - Решение: описание исправления

2. **[WARNING]** Описание проблемы
   ...

### Рекомендации
- Рекомендация 1
- Рекомендация 2

### Предложенные исправления
```python
# Код исправления
```
```

---

## Критерии успеха

1. Раздел "🔐 Управление кошельком" открывается без ошибок
2. Отображается dashboard с балансами кошельков
3. Все кнопки в разделе работают корректно
4. Нет Markdown parse errors в логах
5. Flow авторизации master key работает корректно

---

## Важно

- **НЕ ИЗМЕНЯТЬ** файлы напрямую - только анализ и отчёт
- **НЕ ВКЛЮЧАТЬ** в отчёт реальные значения секретов
- Фокус на **выявлении проблем**, а не на их исправлении
- Каждый агент работает **независимо** от других
